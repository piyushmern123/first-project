{
  "name": "crud-mern",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node \"D:\\New folder (2)\\index.js\"", 
    "build": "npm run build-frontend && npm run build-backend",
    "build-frontend": "cd client && npm install && npm run build",
    "build-backend": "cd node_modules && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^6.12.0"
  }
}
